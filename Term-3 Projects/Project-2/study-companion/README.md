# Study Companion (React + Vite)

This folder contains the React app implementation.

For the full project README (feature overview aligned to `study-companion/3.md`, routes, and setup), see the repository root `README.md`.

From this folder you can run:

```sh
npm install
npm run dev
```
